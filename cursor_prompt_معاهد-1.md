# برومبت البناء الكامل لمنصة "المعاهد" — Django + DRF + PostgreSQL

## تعليمات عامة لك يا كورسر

أنت تبني منصة إدارة معاهد تعليمية باللغة العربية (RTL بالكامل). المشروع Backend فقط الآن (Django + Django REST Framework + PostgreSQL). **يوجد ملف Postman Collection مرفق مع هذا البرومبت — يجب أن تطابق كل الـ API endpoints (المسارات، الأفعال HTTP، أسماء الحقول في الطلب والاستجابة، أكواد الحالة) لهذا الملف تماماً وبدون أي اختلاف. لا تخترع endpoints إضافية ولا تُسمِّ حقلاً بشكل مختلف عمّا هو موجود في الـ Collection.** إذا وجدت أي غموض أو نقص في الـ Collection بالنسبة لميزة موصوفة أدناه، استنتج الحل الأقرب منطقياً لبنية الـ Collection ولا تكسر التوافق معه.

## ⚠️ تأكيد إلزامي وحاسم على مطابقة الـ API — هذا أهم شرط بالمشروع كامل

ملف الـ Postman المرفق هو **مصدر الحقيقة الوحيد (Single Source of Truth)** لكل ما يخص الـ API — وليس هذا البرومبت. عند أي تعارض بين وصف بهذا البرومبت وبين ما هو موجود فعلياً بملف الـ Postman، **اتبع ملف الـ Postman دائماً**. الهدف: أرفع الـ Collection نفسها على تطبيق Postman بعد ما تخلص وتجرب كل request فيها بدون أي تعديل يدوي من طرفي على الرابط أو الحقول أو الـ Body.

التزم حرفياً بكل التالي بدون أي استثناء:
- **المسار (Path)**: حرفياً كما هو، بما فيها أي underscore أو dash أو ترتيب أجزاء الرابط، وحساس لحالة الأحرف (case-sensitive).
- **الفعل (HTTP Method)**: GET/POST/PUT/PATCH/DELETE بالضبط كما محدد بكل request داخل الـ Collection.
- **أسماء الحقول بالـ Request Body**: انسخها حرفياً من نموذج (example) الـ Body الموجود داخل كل request بالـ Collection إن وُجد — لا تُعرّب أو تُبدّل أي اسم حقل، ولا تغيّر بين `camelCase` و`snake_case` عن الاسم الأصلي.
- **بنية الاستجابة (Response Structure)**: إذا كان بالـ Collection نموذج استجابة محفوظ (Saved Response / Example)، اجعل شكل استجابتك الفعلي مطابقاً له تماماً (نفس أسماء المفاتيح، نفس التداخل/nesting، نفس الأنواع).
- **أكواد الحالة (Status Codes)**: طابق كود النجاح المتوقع (200/201/204) وأكواد الخطأ إن كانت موثقة.
- **الـ Headers والمصادقة**: إذا كان الـ Collection يستخدم نمط توكن معين (Bearer Token مثلاً) بصيغة معينة بالـ Headers، اتبع نفس الصيغة بالضبط.
- **لا تُضِف حقولاً إضافية إجبارية** غير موجودة بالـ Collection، ولا تجعل حقلاً اختيارياً بالـ Collection إجبارياً بالـ Serializer.

**خطوة تحقق نهائية إلزامية قبل ما تعتبر المشروع منتهي**: بعد ما تخلص بناء كل الـ endpoints، ارجع اعمل مراجعة request-by-request لكل request موجود بملف الـ Postman كامل (وحدة وحدة، بدون تخطي أي وحدة)، وتأكد فعلياً أن كل واحد فيهم عنده endpoint مطابق تماماً بالكود (نفس المسار، الفعل، الحقول، الاستجابة). لخّصلي بجدول قصير بنهاية شغلك: اسم كل request بالـ Collection، وحالة تطابقه (✅ مطابق تماماً / ⚠️ فيه فرق واضحه إيش هو). هذا الجدول شرط أساسي لاعتبار المهمة مكتملة.

قواعد الكود:
- **كل سطر كود تقريباً (كل دالة، كل شرط، كل استعلام ORM، كل خطوة منطقية) يجب أن يحمل تعليقاً بالعربي فوقه أو بجانبه يشرح وظيفته بوضوح** — الهدف إني أقدر أفهم وأراجع الكود سطر سطر حتى لو ما كتبته أنا. لا تكتفِ بتعليق عام لكل دالة فقط، بل وضّح كل خطوة داخلها.
- التعليقات داخل الكود باللغة العربية، وأسماء المتغيرات والدوال بالإنجليزية.
- استخدم Django ORM حصراً، ممنوع أي SQL خام (raw queries) إلا لضرورة قصوى موثقة.
- استخدم DRF Serializers لكل التحقق من صحة المدخلات (validation).
- بنية المشروع بـ apps منفصلة حسب الوظيفة: `accounts`, `academics` (المراحل/الشعب/المواد), `attendance`, `grades`, `schedule`, `payments`.

---

## 1. نموذج المستخدمين والصلاحيات

- `CustomUser` (يرث من `AbstractBaseUser` أو `AbstractUser`) بحقل `role` يكون واحداً من: `manager`, `teacher`, `student`.
- **الرقم المميز (special number)**: حقل فريد (`unique=True`) لكل من المدير، الأستاذ، والطالب، يُستخدم للتوجيه (routing) عند تسجيل الدخول من الصفحة الرئيسية.
  - لا تُخزّن الرقم المميز كنص صريح بدون حماية — عامله كسرّ حساسية متوسطة: خزّنه عادياً في الداتابيز (لأنه يُعرض للمدير عند البحث) لكن **امنع تخمينه بالقوة الغاشمة** عبر throttling صارم (انظر قسم الأمان).
  - رقم المدير المميز (`7788990` في المتطلبات الأصلية) **يجب ألا يكون Hardcoded في الكود**. اجعله يُزرع عبر Django data migration أو management command يقرأ القيمة من متغير بيئة `ADMIN_SPECIAL_NUMBER`، مع إمكانية تغييره لاحقاً من لوحة التحكم.
- المدير له اسم مستخدم وكلمة مرور إضافة إلى الرقم المميز (مصادقة على مرحلتين: الرقم المميز أولاً، ثم عند دخوله صفحته الرئيسية يسجّل دخول بـ username/password).
- استخدم `djangorestframework-simplejwt` للمصادقة عبر الـ API (access + refresh tokens)، مع مدة صلاحية قصيرة للـ access token (مثلاً 15 دقيقة) وrefresh token أطول مع إمكانية إبطاله (blacklist).

## 2. النماذج الأكاديمية (Academics)

- `Stage` (المرحلة): بكالوريا، حادي عشر، انتقالي، علمي/أدبي، عاشر، تاسع، تامن، سابع... الخ (يمكن جعلها جدول قابل للإدارة بدل قيم ثابتة).
- `Subject` (المادة): رياضيات، علوم، فيزياء، كيمياء، عربي، وطنية، ديانة، انكليزي، فرنسي، جغرافيا، تاريخ، فلسفة...
- `Section` (الشعبة): الشعبة الأولى إلى الخامسة، مرتبطة بمرحلة معينة.
- `Teacher`: مرتبط بـ `CustomUser` (role=teacher)، ويحمل: الاسم الأول، الكنية، الرقم المميز، الجنس، رقم الهاتف، المادة (FK على `Subject`)، وملف الـ CV.
- `Student`: مرتبط بـ `CustomUser` (role=student)، ويحمل: الاسم الأول، الاسم الأخير، الرقم المميز، رقم هاتف الطالب، رقم هاتف الأهل، عنوان السكن، ملاحظات إضافية، ومرتبط بـ `Stage` و`Section`.

## 3. تاسكات صفحة المدير (كلها تحتاج endpoints مطابقة للـ Postman)

1. **إضافة أستاذ**: إنشاء `Teacher` جديد + `CustomUser` مرتبط تلقائياً بـ role=teacher، مع رفع ملف CV.
2. **إضافة طالب**: عملية من خطوتين (المرحلة/الشعبة/المواد ثم بيانات الطالب الشخصية) — صمّمها كـ endpoint واحد يستقبل كل البيانات معاً، أو خطوتين إذا كان الـ Postman يوضح ذلك صراحة.
3. **حذف طالب**: حذف عبر الرقم المميز — استخدم Soft Delete (حقل `is_active=False`) بدل الحذف الفعلي للحفاظ على سجلات الحضور والعلامات والدفعات التاريخية، إلا إذا كان الـ Postman يطلب حذفاً فعلياً صراحة.
4. **تعديل بيانات/شعبة الطالب**: تحديث جزئي (PATCH) لبيانات الطالب.
5. **وضع علامات الطلاب**: نموذج `Grade` (اسم المذاكرة، تاريخ التقديم، الشعبة، وعلامة + ملاحظة لكل طالب).
6. **تعديل البرنامج**: نموذج `ScheduleEntry` (اليوم من الأحد للسبت، المادة، الأستاذ [قائمة منسدلة من الأساتذة المسجلين]، القاعة، الساعة) مرتبط بمرحلة وشعبة.
7. **البحث عن برنامج الأستاذ**: endpoint يرجع كل حصص أستاذ معيّن (فلترة على `ScheduleEntry` حسب `teacher_id`).
8. **الحضور**: نموذج `Attendance` (تاريخ، طالب، حالة حضور/غياب) مرتبط بمرحلة وشعبة.
9. **المدفوعات**: نموذج `Payment` يدعم حالتين: دفعة كاملة، أو تقسيط (`installment`). لكل قسط: القيمة، حالة الحساب (قيد التحصيل/مكتمل)، القسط الكلي، المدفوع، المتبقي. كل عملية إضافة دفعة يجب أن تُنشئ سجل حركة مالية (transaction record) منفصل لأغراض التدقيق.
10. **البحث عن بيانات الطالب**: بحث بالرقم المميز أو الاسم (بحث جزئي `icontains`)، مع دعم عدم إرسال أي معامل لإرجاع كل الطلاب (مع Pagination إجباري لتفادي إرجاع آلاف السجلات دفعة واحدة).

## 4. صفحة الطالب (Read-only بالكامل)

- الطالب يدخل رقمه المميز فقط (بدون كلمة مرور — طبّق throttling صارم جداً هنا لأنه المصادقة الوحيدة).
- Endpoints مخصصة تُرجع **فقط** بيانات الطالب صاحب التوكن: مذاكراته وعلاماته، حضوره، برنامجه الدراسي (فقط لموادّه المسجل بها)، معلومات أستاذه، وحالة الدفع.
- **حرج أمنياً**: يجب أن تفرض الـ permissions على مستوى الكائن (object-level) أن الطالب لا يستطيع أبداً الوصول لبيانات طالب آخر عبر تغيير ID في الرابط (IDOR prevention) — تحقق دوماً أن `request.user.student == target_student` قبل إرجاع أي بيانات.

## 5. متطلبات الأمان الإلزامية (لا تتنازل عن أي بند هنا)

- **متغيرات البيئة**: `SECRET_KEY`, بيانات اتصال PostgreSQL, `ADMIN_SPECIAL_NUMBER`, وأي مفتاح حساس تُقرأ حصراً من `.env` عبر `django-environ` أو `python-decouple`. لا شيء حساس داخل الكود. أنشئ `.env.example` بدون قيم حقيقية.
- **DEBUG=False** في الإنتاج، مع `ALLOWED_HOSTS` محددة صراحة.
- **Throttling/Rate Limiting**: فعّل `DEFAULT_THROTTLE_CLASSES` في DRF، وطبّق حداً صارماً وخاصاً (custom throttle scope) على: تسجيل دخول المدير (username+password)، دخول الرقم المميز (لكل من المدير/الأستاذ/الطالب)، ونقاط الدفع. هذا لمنع هجمات القوة الغاشمة (brute force) على الرقم المميز خاصة لأنه رقم قصير قابل للتخمين.
- **كلمات المرور**: استخدم Django's `AbstractBaseUser` مع `PASSWORD_HASHERS` الافتراضية (Argon2 إن أمكن: `pip install argon2-cffi` وضعه أول hasher). لا تُخزّن أي كلمة مرور بنص صريح أبداً.
- **الصلاحيات (Permissions)**: أنشئ DRF permission classes مخصصة: `IsManager`, `IsTeacher`, `IsStudentOwner` — كل endpoint يجب أن يُقيَّد بالصلاحية المناسبة، ولا تعتمد فقط على إخفاء الرابط في الواجهة.
- **رفع الملفات (CV)**: 
  - تحقق من امتداد ونوع الملف الفعلي (MIME type sniffing وليس فقط الامتداد) — اسمح فقط بـ PDF/DOCX/صور.
  - حد أقصى لحجم الملف (مثلاً 5MB).
  - خزّن الملفات خارج مجلد public مباشر، واعرضها عبر endpoint محمي بصلاحيات وليس رابطاً ثابتاً مباشراً.
  - أعد تسمية الملفات (uuid) لتفادي path traversal وتعارض الأسماء.
- **حماية من SQL Injection**: عبر الالتزام الكامل بـ Django ORM/Serializers، وممنوع بناء أي استعلام بـ string formatting.
- **حماية من XSS**: الاعتماد على auto-escaping في القوالب، وتنظيف أي حقل نصي حر (كالملاحظات) عند العرض.
- **CSRF**: مفعّل لأي جلسة تعتمد Session Authentication؛ إذا كانت الواجهة الأمامية منفصلة تعتمد JWT بالكامل فلا حاجة لـ CSRF على تلك المسارات لكن يجب توضيح ذلك صراحة في الإعدادات.
- **CORS**: استخدم `django-cors-headers` مع `CORS_ALLOWED_ORIGINS` محددة صراحة (بدون `CORS_ALLOW_ALL_ORIGINS=True`).
- **إعدادات أمان Django القياسية**: `SECURE_SSL_REDIRECT`, `SESSION_COOKIE_SECURE`, `CSRF_COOKIE_SECURE`, `SECURE_HSTS_SECONDS`, `X_FRAME_OPTIONS='DENY'`, `SECURE_CONTENT_TYPE_NOSNIFF=True` — كلها مفعّلة في إعدادات الإنتاج.
- **التحقق من صحة المدخلات**: أرقام الهواتف بصيغة محددة (regex)، الرقم المميز رقمي بطول ثابت وفريد، منع تكرار التسجيل بنفس الرقم المميز.
- **السجلات (Logging)**: سجّل محاولات الدخول الفاشلة (بدون تسجيل الرقم المميز أو كلمة المرور نفسها بنص صريح في اللوجز)، ولا تُرجع رسائل خطأ تكشف تفاصيل داخلية (stack traces) للمستخدم النهائي عند `DEBUG=False`.
- **العزل بين الأدوار**: تأكد أن أي endpoint خاص بالطالب لا يمكن الوصول له بتوكن أستاذ أو العكس (اختبر هذا صراحة).

## 6. الاختبارات (Tests)

- اكتب اختبارات وحدة وتكامل (unit + integration) تغطي: تسجيل الدخول بكل الأدوار، كل CRUD أساسي، ومحاولات وصول غير مصرح بها (يجب أن ترجع 403/401).
- اختبر أن كل endpoint موجود في ملف الـ Postman له اختبار مطابق واحد على الأقل.

## 6.5 قيود طول الحقول (Max Length) — إلزامية بالـ Model وبالـ Serializer معاً

طبّق هذه الحدود **بمستويين**: كـ `max_length` على حقل الـ Model نفسه (حماية على مستوى قاعدة البيانات)، **و**كـ validation صريح بالـ Serializer يرجع خطأ 400 واضح بالعربي عند التجاوز (لا تعتمد على خطأ قاعدة البيانات الافتراضي فقط).

**صفحة الدخول الرئيسية (Routing)**
- الرقم المميز (routing عام): 7 خانات كحد أقصى.
- اسم المستخدم الخاص بالمدير: 25 حرفاً.
- كلمة المرور: 25 حرفاً.

**إضافة أستاذ**
- الاسم الأول: 15 حرفاً.
- الاسم الأخير: 15 حرفاً.
- الرقم المميز + تأكيد الرقم المميز: 10 خانات.
- رقم الأستاذ (رقم هاتفه): 10 خانات.
- مادته: 30 حرفاً.
- السيرة المهنية (نص مختصر، بالإضافة لملف الـ CV): 175 حرفاً.

**تسجيل طالب**
- الاسم الأول + الاسم الأخير: 15 حرفاً لكل منهما.
- الرقم المميز + تأكيد الرقم المميز: 10 خانات.
- رقم هاتف الطالب + رقم هاتف الأهل: 10 خانات لكل منهما.
- عنوان السكن: 30 حرفاً.
- ملاحظات إضافية: 25 حرفاً.

**حذف طالب**
- الرقم المميز: 10 خانات.

**تغيير إعدادات طالب**
- الرقم المميز: 10 خانات.
- باقي الحقول: نفس حدود "تسجيل طالب" أعلاه بالضبط (نفس الحقول تُعاد تعبئتها).

**المدفوعات**
- الرقم المميز: 10 خانات.

**البحث عن بيانات طالب**
- الرقم المميز: 10 خانات.

**ملاحظة مهمة**: لاحظ أن الرقم المميز للمدير (بصفحة الدخول الرئيسية) حده 7 خانات، بينما الرقم المميز للأستاذ/الطالب حده 10 خانات — هذا فرق مقصود بين النوعين، لا توحّدهما بنفس القيد.

## 6.6 إعدادات كوكيز الجلسة (Session) — لتفادي حلقة إعادة التوجيه بـ /admin/

**تنبيه**: نسخ رابط `/admin/` وفتحه بمتصفح آخر أو نافذة تصفح خفي (Incognito) دايماً رح يطلب تسجيل دخول من جديد — هذا سلوك أمني طبيعي ومقصود بكل نظام مبني على Session (الجلسة مربوطة بكوكيز خاصة بكل متصفح)، وليس خللاً بالكود، ولا داعي لمحاولة "حله" بمعنى تجاوزه.

**لكن** يوجد سبب برمجي شائع يُنتج نفس العرض حتى بنفس المتصفح، لازم نتفاداه صراحة:
- إذا فُعّل `SESSION_COOKIE_SECURE=True` (مطلوب حسب قسم الأمان أعلاه) بينما الموقع لسا يعمل على HTTP بدون شهادة SSL، فالمتصفح **لن يحفظ الكوكي إطلاقاً**، فتصير حلقة توجيه دائمة لصفحة الدخول حتى بعد إدخال بيانات صحيحة.
- **الحل الإلزامي بالكود**: اجعل `SESSION_COOKIE_SECURE` و`CSRF_COOKIE_SECURE` مشروطين بمتغير بيئة واحد (مثلاً `IS_HTTPS=True/False` أو الاعتماد على `DEBUG`)، بحيث تكونان `True` فقط عندما يعمل السيرفر فعلياً خلف HTTPS، و`False` أثناء التطوير المحلي أو أي نشرة لسا بلا SSL. وثّق هذا صراحة بـ `DEPLOYMENT.md` مع تنبيه: "لا تفعّل هذا الإعداد قبل التأكد من تركيب شهادة SSL على السيرفر، وإلا لن تتمكن من تسجيل الدخول لـ /admin/ نهائياً".
- كذلك تأكد أن `SESSION_COOKIE_DOMAIN` متوافق مع النطاق (domain) الفعلي المستخدم بكل نشرة/معهد، ولا يُترك بقيمة ثابتة بالكود.

### 6.6.1 حماية إضافية لجلسة المدير (Session Hardening)

بما أن حساب المدير حساس، أضف هذه الطبقات الإضافية بغض النظر عن سيناريو المزامنة بين الأجهزة:

- **مدة صلاحية قصيرة للجلسة**: اضبط `SESSION_COOKIE_AGE` بحيث تنتهي الجلسة تلقائياً بعد **نصف ساعة (30 دقيقة) من الخمول فقط**، وفعّل `SESSION_SAVE_EVERY_REQUEST=True` بحيث يتجدد العداد مع كل نشاط فعلي بدل انتهاء الجلسة أثناء استخدام فعلي للوحة.
- **جلسة واحدة نشطة فقط لكل مدير**: عند تسجيل دخول ناجح من جهاز/متصفح جديد، أبطل تلقائياً أي جلسة سابقة نشطة لنفس حساب المدير (احذف مفاتيح الجلسات القديمة المرتبطة بهذا المستخدم من جدول الجلسات، أو استخدم حقلاً بالـ Model لتخزين "آخر مفتاح جلسة صالح" والتحقق منه بكل طلب عبر middleware مخصص). الهدف: إذا كان هناك جهاز مسجّل دخول لا يعرفه المدير، ينقطع تلقائياً بمجرد ما يسجّل المدير دخول من جهازه.
- **تسجيل كل عملية دخول ناجحة (Login Audit Log)**: أنشئ نموذج `LoginLog` (أو ما يعادله) يسجّل لكل تسجيل دخول ناجح للمدير: التاريخ والوقت، عنوان IP، الـ User-Agent (نوع الجهاز/المتصفح). اعرض هذا السجل عبر Django Admin (`list_display` بسيط) بحيث يقدر المدير يراجع آخر عمليات الدخول لحسابه ويكتشف أي دخول مشبوه لم يقم به هو.

## 7. نظام SaaS — الاشتراك و Multi-Tenancy (نسخة مبسّطة)

المشروع لازم يدعم عدة معاهد (Tenants) مستقلين تماماً عن بعض، عبر **نفس الكود (نفس الـ codebase) منشور بعدة نُسخ نشر (deployments) منفصلة** — وليس كوداً مكرراً فعلياً. كل معهد إله نشره الخاص (سيرفر/عملية Django خاصة، قاعدة بيانات PostgreSQL خاصة به بالكامل، ملف `.env` خاص فيه بـ `SECRET_KEY` منفصل وبيانات اتصال قاعدة البيانات الخاصة فيه).

**مهم**: أي تعديل أو إصلاح ثغرة أمنية يُطبَّق مرة واحدة على الكود المشترك، ثم يُعاد نشره (redeploy) على كل نسخ المعاهد — لا يجوز أبداً تفريع (fork) الكود لكل معهد لتفادي انحراف النسخ عن بعضها (code drift) وتراكم ثغرات غير مُصلحة في بعض النسخ.

### 7.1 نموذج الاشتراك (بسيط جداً — تاريخ انتهاء فقط)
- **لا يوجد نظام اشتراك معقد، ولا خطط، ولا حدود لعدد الأساتذة أو الطلاب.** المطلوب فقط: حقل تاريخ واحد يحدد متى يتوقف المعهد عن العمل.
- نموذج `Subscription` بسيط جداً (داخل قاعدة بيانات المعهد نفسها) يحتوي على حقل واحد أساسي: `expiry_date` (تاريخ الانتهاء). يمكن إضافة حقل `is_active` (Boolean، افتراضياً True) لإتاحة إيقاف يدوي فوري إن احتجت بدون تعديل التاريخ.
- **الفحص إجبارياً على مستوى الـ Backend**: أنشئ permission class أو middleware بسيط يتحقق قبل أي endpoint محمي (باستثناء مسارات تسجيل الدخول نفسها إن احتجت السماح برسالة واضحة) أن: تاريخ اليوم لم يتجاوز `expiry_date`، و`is_active=True`. إذا تجاوز التاريخ أو كان `is_active=False`: يُمنع الدخول لكل مستخدمي هذا المعهد (مدير/أستاذ/طالب) برسالة واضحة بالعربي (مثلاً "انتهت صلاحية اشتراك هذا المعهد، يرجى التواصل مع الدعم") بدل خطأ عام (403).
- لا يوجد أي workflow تجديد أو دفع داخل النظام. تمديد الاشتراك يتم فقط بتعديل قيمة `expiry_date` يدوياً من طرفك مباشرة.

### 7.2 الإدارة — Django Admin الافتراضي فقط
- **لا حاجة لبناء لوحة Super Admin منفصلة ولا endpoints خاصة بها.** استخدم Django Admin الجاهز (`/admin/`) لكل نشرة/معهد على حدة.
- سجّل نموذج `Subscription` في `admin.py` بشكل مباشر (`admin.site.register`)، مع `list_display` بسيط يعرض `expiry_date` و`is_active` بوضوح.
- الدخول لـ `/admin/` يتطلب حساب `is_superuser=True` منفصل عن حسابات المدير/الأستاذ/الطالب العاديين — هذا الحساب يُنشأ عبر `createsuperuser` عند نشر كل معهد، وبياناته لا تُخزَّن بأي مكان بالكود.
- **تشديد أمني على `/admin/`**: غيّر المسار الافتراضي `admin/` لمسار عشوائي غير متوقع عبر متغير بيئة، فعّل throttling على محاولات الدخول له، وقيّد الوصول له إن أمكن بعنوان IP محدد (allowlist) على مستوى السيرفر (Nginx) بالإضافة لمصادقة Django نفسها.
- تمديد أو إيقاف الاشتراك يتم بتعديل مباشر لحقل `expiry_date` (أو `is_active`) من نفس صفحة Django Admin هاي — بدون أي واجهة أو API إضافية.

### 7.3 النشر (Deployment) — نفس الكود، عدة سيرفرات
- المشروع يُرفع على **GitHub كـ repo واحد فقط**. كل معهد إله سيرفر/استضافة خاصة فيه، وبيسحب (`clone`/`pull`) نفس الكود، ويشغّله بـ `.env` خاص فيه (قاعدة بيانات مختلفة، `SECRET_KEY` مختلف، `ALLOWED_HOSTS` مختلف).
- أنشئ ملف **`DEPLOYMENT.md`** بخطوات واضحة ومرقّمة قابلة للتكرار على أي سيرفر جديد، تشمل:
  1. `git clone` أو `git pull` للريبو.
  2. إنشاء بيئة افتراضية وتثبيت `requirements.txt`.
  3. إنشاء `.env` من `.env.example` وتعبئة القيم الخاصة بهذا المعهد (قاعدة بيانات، secret key، مسار admin العشوائي إن وُجد).
  4. إنشاء قاعدة بيانات PostgreSQL فارغة لهذا المعهد.
  5. تشغيل `python manage.py migrate`.
  6. تشغيل `python manage.py createsuperuser` لإنشاء حساب Django Admin الخاص بهذا المعهد.
  7. أوامر التشغيل (gunicorn/uwsgi + nginx إن وجدت) وإعادة التشغيل بعد أي تحديث.
- أنشئ سكربت بسيط **`deploy.sh`** (Bash) يشغَّل على كل سيرفر لتسهيل عملية تحديث الكود لاحقاً، يقوم بـ: `git pull` → تفعيل البيئة الافتراضية → `pip install -r requirements.txt` → `python manage.py migrate` → `python manage.py collectstatic --noinput` → إعادة تشغيل خدمة التطبيق (مثلاً `systemctl restart gunicorn` أو ما يعادلها). اجعل أوامر إعادة التشغيل قابلة للتخصيص بسهولة (متغير بأعلى السكربت) لأنها قد تختلف بين سيرفر وآخر.
- وضّح داخل `DEPLOYMENT.md` أن أي تعديل على الكود (خصوصاً إصلاحات أمنية) يُدفع مرة واحدة إلى GitHub، ثم يُطبَّق يدوياً عبر `deploy.sh` على كل سيرفر معهد على حدة — مع تنبيه أن نسيان تحديث أحد السيرفرات يعني بقاءه على نسخة قديمة وربما بها ثغرة غير مُصلحة.

## 8. التسليمات المطلوبة

- بنية مشروع Django منظمة بـ apps منفصلة كما ذُكر.
- `requirements.txt` كامل.
- `.env.example`.
- ملف `README.md` يشرح خطوات التشغيل المحلي والنشر (venv + gunicorn، بدون Docker).
- Migrations كاملة تشمل data migration لزرع المدير الأولي.
- توثيق مختصر لكل endpoint (يمكن الاكتفاء بمطابقته لملف Postman دون توثيق إضافي منفصل إن كان الوقت لا يسمح).

---

**ابدأ الآن بقراءة ملف Postman collection المرفق أولاً بالكامل، ثم اعمل الـ API مطابقة تماماً للملف — كل مسار وكل فعل HTTP وكل حقل بالضبط زي ما هو موجود فيه، بدون أي اختلاف أو إضافة أو حذف. ثم ابدأ ببناء المشروع تدريجياً: الإعدادات والأمان أولاً، ثم accounts، ثم باقي الـ apps بالترتيب المذكور أعلاه.**
