# مكتبات منصة المعاهد — ثبّتها بـ: pip install -r requirements.txt
Django>=5.1,<5.3
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.4
django-environ>=0.11
dj-database-url>=2.1

# ── مشغّل PostgreSQL (psycopg 3) ──
# على x86_64/macOS يُثبَّت الويل الجاهز فلا يوجد بناء C إطلاقاً.
psycopg[binary]>=3.1,<4; platform_machine in "x86_64 AMD64 arm64"
# على Termux/ARM لا توجد عجلات binary، فنستخدم النسخة النقية (Python خالص)
# وهي تحتاج libpq من النظام: pkg install postgresql
psycopg>=3.1,<4; platform_machine in "aarch64 armv7l armv8l"

argon2-cffi>=23.1
gunicorn>=22.0
Pillow>=10.0
